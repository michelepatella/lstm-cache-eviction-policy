apiVersion: v1
kind: Service
metadata:
  name: featurizer-service
  namespace: featurizer-service
spec:
  selector:
    app: featurizer-service
    role: featurizer-service
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8001
