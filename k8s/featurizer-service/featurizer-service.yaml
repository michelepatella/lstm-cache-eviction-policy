apiVersion: apps/v1
kind: Deployment
metadata:
  name: featurizer-service
  namespace: featurizer-service
  labels:
    app: featurizer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: featurizer-service
  template:
    metadata:
      labels:
        app: featurizer-service
    spec:
      containers:
      - name: featurizer-service
        image: lstmcacheevictionpolicy.azurecr.io/lstm-cache-eviction-policy-featurizer_service:latest
        ports:
        - containerPort: 8001
        envFrom:
        - configMapRef:
            name: featurizer-service-config
        securityContext:
          runAsNonRoot: true
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: "5m"
            memory: "50Mi"
          limits:
            cpu: "50m"
            memory: "250Mi"
      imagePullSecrets:
        - name: acr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: featurizer-service
  namespace: featurizer-service
spec:
  selector:
    app: featurizer-service
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8001
