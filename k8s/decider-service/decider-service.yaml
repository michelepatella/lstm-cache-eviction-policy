apiVersion: v1
kind: Service
metadata:
  name: decider-service
  namespace: decider-service
spec:
  selector:
    app: decider-service
    role: decider-service
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8004
