apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: predictor-service
spec:
  podSelector:
    matchLabels:
      app: predictor-service
  policyTypes:
    - Ingress
    - Egress
