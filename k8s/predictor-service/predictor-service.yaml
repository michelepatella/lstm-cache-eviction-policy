apiVersion: v1
kind: Service
metadata:
  name: predictor-service
  namespace: predictor-service
spec:
  selector:
    app: predictor-service
    role: predictor-service
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8002
