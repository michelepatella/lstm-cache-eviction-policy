apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: scorer-service
spec:
  podSelector:
    matchLabels:
      app: scorer-service
  policyTypes:
    - Ingress
    - Egress
