apiVersion: apps/v1
kind: Deployment
metadata:
  name: scorer-service
  namespace: scorer-service
  annotations:
    kube-score/ignore: pod-networkpolicy,networkpolicy-targets-pod
  labels:
    app: scorer-service
    role: scorer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scorer-service
      role: scorer-service
  template:
    metadata:
      annotations:
        kube-score/ignore: pod-networkpolicy,networkpolicy-targets-pod
      labels:
        app: scorer-service
        role: scorer-service
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
        cloud.google.com/compute-class: Scale-Out
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 10001
        runAsGroup: 10001
        runAsNonRoot: true
        fsGroup: 2000
      containers:
      - name: scorer-service
        image: europe-west4-docker.pkg.dev/lstm-cache-eviction-policy/lstm-cache-eviction-policy-cr/scorer-service:PLACEHOLDER_SHA
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        readinessProbe:
          exec:
            command:
              - sh
              - -c
              - "echo ready"
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          exec:
            command:
              - sh
              - -c
              - "echo ok"
          initialDelaySeconds: 10
          periodSeconds: 15
        envFrom:
        - configMapRef:
            name: scorer-service-config
        securityContext:
          readOnlyRootFilesystem: true
          runAsUser: 10001
          runAsGroup: 10001
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
            ephemeral-storage: "100Mi"
          limits:
            cpu: "250m"
            memory: "512Mi"
            ephemeral-storage: "100Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: scorer-service
  namespace: scorer-service
spec:
  selector:
    app: scorer-service
    role: scorer-service
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8003
